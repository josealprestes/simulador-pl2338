/**
 * Versão única do Simulador PL 2338/2023.
 *
 * Fonte de verdade: package.json (via config/version). Usada na capa,
 * rodapés e metadados dos relatórios (LaTeX e PDF).
 */
import { APP_VERSION } from "@/config/version";

export const APP_NAME = "Simulador PL 2338/2023";
export { APP_VERSION };
